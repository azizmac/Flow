# Знак Flow в Flow.Auth

Копия из `Flow.Client/wwwroot/brand/`. Сервисы намеренно не ссылаются друг на
друга (та же причина, по которой в `auth.css` продублированы токены палитры),
поэтому favicon лежит здесь отдельно. Менять вместе с клиентом.

Источник правды и полный комплект — `src/Flow.Client/wwwroot/brand/`.
